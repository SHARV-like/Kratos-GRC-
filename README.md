# GRC Hackathon

Project scaffold for the GRC hackathon team.

## Development

Backend:

```bash
cd backend
npm install
npm run dev
```

Frontend:

```bash
cd frontend
npm install
npm run dev
```

Copy `backend/.env.example` to `backend/.env` for local configuration. Do not commit real `.env` files.
